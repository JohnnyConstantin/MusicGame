Документация к генератору мелодий:

Возможности:
- Выбор аккордов для мелодии из списка доступных аккордов
- Выбор длины генерируемой мелодии

Поля ввода пользователя:
- Аккорды (без пробелов, заглавными английскими буквами, как в списке доступных аккордов)
- Длина мелодии (int число)

Классы:
- Menu() \\Взаимодействие с  пользователем, инициализация списка аккордов для мелодии и длины музыки
- MusicActions() \\Класс, отвечающий за действия с мелодией. На данный момент только принтует
- Generator(MusicActions) \\Класс, генерирующий мелодию из полученных аккордов и длины мелодии. Расширяет класс MusicActions
- Test(unittest.TestCase) \\Класс для тестов